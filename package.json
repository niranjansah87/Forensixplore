{
  "scripts": {
    "start": "concurrently \"cd backend && nodemon server.js\" \"cd forensixplore && npm run dev\"",
    "backend": "cd backend && nodemon index.js",
    "frontend": "cd forensixplore && npm run dev"
  },

  "dependencies": {
    "concurrently": "^8.2.2"
  }
}


